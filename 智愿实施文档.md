# 智愿 × Yuxi 实施文档（已完成编码，待动态联调）

> 最后更新：2026-07-29 | 状态：全部代码已编写 + 单测通过，等待环境启动后动态验证

## Yuxi架构分析结果

```
yuxi/
├── backend/
│   ├── package/yuxi/          ← 核心包（pip install -e 安装）
│   │   ├── agents/            ← Agent框架
│   │   │   ├── base.py        ← BaseAgent基类（LangGraph图构建）
│   │   │   ├── buildin/       ← 内置Agent（chatbot/subagent）
│   │   │   │   └── chatbot/
│   │   │   │       ├── graph.py   ← create_agent() + middleware栈
│   │   │   │       ├── prompt.py  ← System Prompt构建
│   │   │   │       ├── state.py   ← 状态Schema
│   │   │   │       └── context.py ← 运行时上下文
│   │   │   ├── toolkits/      ← 工具注册系统
│   │   │   │   ├── registry.py    ← @tool装饰器（核心！）
│   │   │   │   ├── service.py     ← resolve_configured_runtime_tools()
│   │   │   │   ├── buildin/tools.py ← 内置工具（OCR/搜索/提问）
│   │   │   │   └── kbs/tools.py   ← 知识库7个工具
│   │   │   ├── skills/        ← Skill系统（SKILL.md描述文件）
│   │   │   ├── middlewares/   ← 中间件（摘要/技能门控/子Agent）
│   │   │   └── backends/      ← 后端能力（沙盒/知识库/技能）
│   │   ├── knowledge/         ← RAG Pipeline
│   │   │   ├── parser/        ← 文档解析（MinerU/PaddleX）
│   │   │   ├── chunking/      ← 分块策略
│   │   │   ├── graphs/        ← 知识图谱（Neo4j）
│   │   │   └── implementations/ ← Milvus/pgvector实现
│   │   ├── models/            ← LLM Provider抽象
│   │   ├── services/          ← 业务服务（chat/conversation/agent_run）
│   │   ├── repositories/      ← 数据访问层
│   │   ├── storage/           ← PostgreSQL管理器
│   │   └── config/            ← 配置
│   └── server/                ← FastAPI应用
│       ├── main.py
│       └── routers/           ← 21个路由文件
│           ├── chat_router.py
│           ├── knowledge_router.py
│           ├── agent_router.py
│           ├── tool_router.py
│           └── ...
├── web/src/                   ← Vue3前端
│   ├── views/
│   ├── components/
│   ├── apis/
│   ├── stores/
│   └── router/
└── docker-compose.yml         ← 部署（PG+Redis+Milvus+Neo4j+MinIO）
```

---

## 核心机制（已验证）

### 工具注册

```python
# yuxi/agents/toolkits/registry.py
from yuxi.agents.toolkits.registry import tool

@tool(category="buildin", tags=["搜索"], display_name="Tavily 网页搜索")
def my_tool(query: str) -> str:
    """工具描述（LLM看的）"""
    ...
```

装饰器自动：注册到全局列表 → 附加元数据 → 设置handle_tool_error=True

### 工具绑定到Agent

```python
# yuxi/agents/toolkits/service.py → resolve_configured_runtime_tools(context)
# 来源1: context.tools → buildin工具按name匹配
# 来源2: context.mcps → MCP server工具
# 来源3: resolve_skill_gated_tools(context) → Skill依赖的工具
```

### Agent构建

```python
# yuxi/agents/buildin/chatbot/graph.py
graph = create_agent(
    model=load_chat_model(fully_specified_name=model_spec),
    tools=await resolve_configured_runtime_tools(context),
    system_prompt=build_prompt_with_context(context),
    middleware=[SteerMiddleware(), SkillsMiddleware(), SummaryMiddleware(), ...],
    state_schema=ChatBotState,
    checkpointer=await self._get_checkpointer(),
)
```

### 知识库工具（已有7个）

list_kbs / query_kb / open_kb_document / find_kb_document / get_mindmap / search_file / download_kb_file

---

## 智愿业务注入方案（逐文件）

### 1. 新增工具文件

**创建**: `backend/package/yuxi/agents/toolkits/buildin/zhiyuan_tools.py`

```python
"""智愿 - 高考志愿填报工具集"""
from yuxi.agents.toolkits.registry import tool

# ===== 查询类 =====

@tool(category="zhiyuan", tags=["志愿填报"], display_name="查历年录取分")
async def query_admission_scores(university_name: str = "", province: str = "", ...) -> str:
    """查询院校/专业的历年录取分数线..."""

@tool(category="zhiyuan", tags=["志愿填报"], display_name="查位次")
async def get_score_rank(score: int, province: str, year: int, subject_type: str) -> str:
    """根据分数查询一分一段表..."""

@tool(category="zhiyuan", tags=["志愿填报"], display_name="院校详情")
async def get_university_detail(university_name: str) -> str:
    ...

@tool(category="zhiyuan", tags=["志愿填报"], display_name="招生计划")
async def get_province_plan(university_name: str, province: str, year: int) -> str:
    ...

@tool(category="zhiyuan", tags=["志愿填报"], display_name="就业数据")
async def get_employment_data(university_name: str, major_name: str) -> str:
    ...

@tool(category="zhiyuan", tags=["志愿填报"], display_name="知识图谱查询")
async def query_graph(start_entity: str, relation_type: str, depth: int = 2) -> str:
    ...

# ===== 计算类 =====

@tool(category="zhiyuan", tags=["志愿填报"], display_name="冲稳保推荐")
async def recommend_schools(rank: int, province: str, subject_type: str, strategy: str = "all") -> str:
    ...

@tool(category="zhiyuan", tags=["志愿填报"], display_name="录取概率")
async def calculate_probability(rank: int, university_id: int, years: int = 3) -> str:
    ...

@tool(category="zhiyuan", tags=["志愿填报"], display_name="选科检查")
async def check_subject_requirement(subject_combination: str, province: str) -> str:
    ...

@tool(category="zhiyuan", tags=["志愿填报"], display_name="专业对比")
async def compare_majors(major_names: list[str], university_name: str = "") -> str:
    ...

@tool(category="zhiyuan", tags=["志愿填报"], display_name="位次趋势")
async def rank_trend_analysis(university_id: int, province: str, years: int = 5) -> str:
    ...

# ===== 生成类 =====

@tool(category="zhiyuan", tags=["志愿填报"], display_name="生成志愿方案")
async def generate_application_plan(profile_json: str) -> str:
    """汇总用户画像，生成完整冲稳保志愿方案表..."""

@tool(category="zhiyuan", tags=["志愿填报"], display_name="导出方案")
async def export_plan(plan_json: str, format: str = "pdf") -> str:
    ...
```

### 2. 注册工具（让系统发现）

**修改**: `backend/package/yuxi/agents/toolkits/buildin/__init__.py`

```python
# 在现有import后追加：
from yuxi.agents.toolkits.buildin import zhiyuan_tools  # noqa: F401
```

这一行就够了。`@tool`装饰器在模块加载时自动注册到全局列表。

### 3. 新增Skill描述文件

**创建**: `backend/package/yuxi/agents/skills/buildin/zhiyuan/SKILL.md`

```markdown
---
name: zhiyuan
slug: zhiyuan
description: "高考志愿填报智能顾问。当用户询问高考志愿、院校推荐、录取分数、位次查询、专业选择、填报策略时使用此技能。"
---

# 志愿填报技能

## 可用工具
- query_admission_scores: 查历年录取分
- get_score_rank: 分数→位次
- get_university_detail: 院校详情
- get_province_plan: 招生计划
- get_employment_data: 就业数据
- query_graph: 知识图谱（院校-专业-职业关系）
- recommend_schools: 冲稳保推荐
- calculate_probability: 录取概率
- check_subject_requirement: 选科过滤
- compare_majors: 专业对比
- rank_trend_analysis: 位次趋势
- generate_application_plan: 生成志愿方案
- export_plan: 导出PDF/Excel

## 操作流程
1. 确认用户三要素：省份、科类、分数（缺任何一个必须追问）
2. 有分数后立即调 get_score_rank 获取位次
3. 推荐时调 recommend_schools，必须附位次对比
4. 政策类问题走知识库 query_kb，禁止凭记忆
5. 用户要"出方案"时调 generate_application_plan

## 硬规则
- 推荐前必须有rank
- 政策必须走知识库检索
- 推荐必须附数据依据
- 工具查不到就说"无数据"，不编造
- 方案生成后检查：志愿数合规、无选科冲突、冲稳保比例合理
```

### 4. 修改System Prompt

**修改**: `backend/package/yuxi/agents/buildin/chatbot/prompt.py`

在`build_prompt_with_context()`中，当agent配置了zhiyuan skill时，注入志愿填报专用prompt：

```python
ZHIYUAN_SYSTEM_PROMPT = """你是"智愿"——专业的高考志愿填报AI顾问。

核心能力：查分数、算位次、推荐院校、检索政策、生成志愿方案。

工作原则：
1. 所有数据判断必须基于工具查询结果，绝不编造
2. 推荐时给出位次对比+概率+趋势
3. 缺省份/分数/科类时主动追问
4. 政策类必须走知识库检索
5. 回答简洁实用，像经验丰富的填报老师
"""
```

### 5. 数据层（新增表）

**创建**: `backend/package/yuxi/repositories/zhiyuan_models.py`

用SQLAlchemy定义7张表（universities, colleges, majors, admission_scores, score_ranks, province_rules, enrollment_plans），连接到Yuxi已有的PostgreSQL（`yuxi.storage.postgres.manager.pg_manager`）。

### 6. 数据访问

**创建**: `backend/package/yuxi/repositories/zhiyuan_repository.py`

```python
class ZhiyuanRepository:
    async def query_scores(self, filters) -> list[dict]: ...
    async def get_rank(self, score, province, year, subject) -> dict: ...
    async def get_university(self, name) -> dict: ...
    async def recommend(self, rank, province, subject, strategy) -> dict: ...
    ...
```

工具函数内部调用这个Repository，不直接写SQL。

### 7. 知识图谱

Yuxi已有Neo4j集成（`yuxi/knowledge/graphs/`）。新增院校-专业-职业关系图谱：
- 通过管理后台导入
- 工具`query_graph`调用Yuxi的图谱查询接口

### 8. 前端新增页面

**创建**: `web/src/views/zhiyuan/`
- `PlanView.vue` — 志愿方案展示（表格+冲稳保分组+导出）
- `UniversityBrowse.vue` — 院校库浏览
- `GraphExplore.vue` — 图谱可视化

**修改**: `web/src/router/` — 添加路由

### 9. 管理端

Yuxi已有知识库管理后台（上传文档→解析→向量化→检索）。智愿的政策文档直接用它。
结构化数据导入（院校/分数）新增一个管理页面或API。

---

## 不需要改的（Yuxi直接提供）

| 能力 | Yuxi已有 | 不用动 |
|------|----------|--------|
| 对话UI（流式/多轮/Markdown） | web/src/views/chat | ✓ |
| 知识库管理（上传/解析/检索） | knowledge/ + knowledge_router | ✓ |
| 文档解析（MinerU/PaddleX/OCR） | knowledge/parser/ | ✓ |
| 向量检索（Milvus） | knowledge/implementations/ | ✓ |
| 知识图谱（Neo4j） | knowledge/graphs/ | ✓ |
| 用户/租户/权限 | services/auth + repositories | ✓ |
| 异步任务（ARQ worker） | server/worker_main.py | ✓ |
| LLM Provider切换 | models/providers/ | ✓ |
| 对话摘要/上下文压缩 | middlewares/summary | ✓ |
| MCP工具扩展 | agents/mcp/ | ✓ |
| 文件沙盒 | agents/backends/sandbox/ | ✓ |

---

## 实施状态（2026-07-29 更新）

| 步骤 | 做什么 | 状态 |
|------|--------|------|
| 1 | 配置 .env（API密钥+服务凭证） | ✅ 完成 |
| 2 | 写 zhiyuan_tools.py（13个工具+缓存+异常兜底） | ✅ 完成 |
| 3 | 写 zhiyuan_models.py（7张表）+ zhiyuan_repository.py | ✅ 完成 |
| 4 | 写 SKILL.md + 注册到 __init__.py | ✅ 完成 |
| 5 | 改 prompt.py 注入志愿填报人设 | ✅ 完成 |
| 6 | 写 zhiyuan_router.py（用户端API + 管理端CRUD） | ✅ 完成 |
| 7 | 写 import_zhiyuan_data.py（种子数据脚本） | ✅ 完成 |
| 8 | 写 seed_zhiyuan_graph.py（Neo4j图谱种子） | ✅ 完成 |
| 9 | 前端3个页面 + API模块 + 路由 | ✅ 完成 |
| 10 | 单元测试（15个用例，SQLite内存库） | ✅ 15/15 通过 |
| 11 | docker compose up + 动态联调 | ⏳ 等环境释放 |
| 12 | 上传政策文档到知识库 | ⏳ 待操作 |

---

## 关键文件清单（实际已创建/修改）

### 新建（12个文件）
```
yuxi/.env                                                              ← API密钥+服务凭证
backend/package/yuxi/agents/toolkits/buildin/zhiyuan_tools.py          ← 13个工具（含TTL缓存+异常兜底）
backend/package/yuxi/agents/skills/buildin/zhiyuan/SKILL.md            ← Skill门控描述
backend/package/yuxi/repositories/zhiyuan_models.py                    ← 7张表（SQLAlchemy）
backend/package/yuxi/repositories/zhiyuan_repository.py                ← 数据访问层（冲稳保算法）
backend/server/routers/zhiyuan_router.py                               ← 用户端API + 管理端CRUD（20+接口）
backend/scripts/import_zhiyuan_data.py                                 ← PostgreSQL种子数据（24校/3省/3年）
backend/scripts/seed_zhiyuan_graph.py                                  ← Neo4j图谱种子（院校-专业-职业）
backend/test/unit/repositories/test_zhiyuan_repository.py              ← 单元测试（15用例）
web/src/apis/zhiyuan_api.js                                            ← 前端API封装
web/src/views/zhiyuan/PlanView.vue                                     ← 志愿方案页
web/src/views/zhiyuan/UniversityBrowse.vue                             ← 院校库页
web/src/views/zhiyuan/GraphExplore.vue                                 ← 图谱探索页
```

### 修改（4个文件）
```
backend/package/yuxi/agents/toolkits/buildin/__init__.py               ← 加 from . import zhiyuan_tools
backend/package/yuxi/agents/buildin/chatbot/prompt.py                  ← 加 ZHIYUAN_SYSTEM_PROMPT + 条件注入
backend/server/routers/__init__.py                                     ← 注册 zhiyuan + zhiyuan_admin 路由
web/src/router/index.js                                                ← 加 /zhiyuan/* 三条路由
```

---

## 启动顺序（环境就绪后）

```bash
# 1. 启动基础设施
cd yuxi && docker compose up -d

# 2. 导入种子数据（PostgreSQL）
cd backend && python scripts/import_zhiyuan_data.py

# 3. 导入图谱数据（Neo4j）
cd backend && python scripts/seed_zhiyuan_graph.py

# 4. 启动后端
cd backend && uvicorn server.main:app --reload --port 8000

# 5. 启动前端
cd web && npm run dev

# 6. 访问
# 对话：http://localhost:5173/agent（配置zhiyuan skill后对话）
# 院校库：http://localhost:5173/zhiyuan/universities
# 志愿方案：http://localhost:5173/zhiyuan/plan
# 图谱：http://localhost:5173/zhiyuan/graph
# 管理API：http://localhost:8000/api/zhiyuan/admin/universities
```

---

## 工具层增强（已实现）

- **TTL缓存**：`_resolve_university()` 对院校名称→ID映射做120秒缓存，同一轮对话多次调用不重复查库
- **异常兜底**：`_with_repo()` 包裹所有DB操作，工具层永不抛异常，返回错误文本给LLM
- **结构化日志**：所有异常通过 `logger.error("[zhiyuan_tool] ...")` 记录，方便排查

---

## 管理端 API 清单（zhiyuan_router.py）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/zhiyuan/universities | 搜索院校 |
| GET | /api/zhiyuan/universities/{id} | 院校详情+专业 |
| GET | /api/zhiyuan/scores | 历年录取分 |
| GET | /api/zhiyuan/rank | 一分一段 |
| POST | /api/zhiyuan/recommend | 冲稳保推荐 |
| POST | /api/zhiyuan/plan | 生成志愿方案 |
| GET | /api/zhiyuan/graph | 图谱查询 |
| GET | /api/zhiyuan/rules/{province} | 省份规则 |
| POST | /api/zhiyuan/majors/compare | 专业对比 |
| GET | /api/zhiyuan/subject-check | 选科检查 |
| GET/POST/PUT/DELETE | /api/zhiyuan/admin/universities | 院校CRUD |
| GET/POST/DELETE | /api/zhiyuan/admin/majors | 专业CRUD |
| GET/POST/DELETE | /api/zhiyuan/admin/scores | 分数CRUD+批量导入 |
| GET/POST | /api/zhiyuan/admin/rules | 省份规则 |
| GET/POST | /api/zhiyuan/admin/plans | 招生计划 |

---

## 不需要改的（Yuxi直接提供）

| 能力 | Yuxi已有 | 不用动 |
|------|----------|--------|
| 对话UI（流式/多轮/Markdown） | web/src/views/chat | ✓ |
| 知识库管理（上传/解析/检索） | knowledge/ + knowledge_router | ✓ |
| 文档解析（MinerU/PaddleX/OCR） | knowledge/parser/ | ✓ |
| 向量检索（Milvus） | knowledge/implementations/ | ✓ |
| 知识图谱（Neo4j） | knowledge/graphs/ | ✓ |
| 用户/租户/权限 | services/auth + repositories | ✓ |
| 异步任务（ARQ worker） | server/worker_main.py | ✓ |
| LLM Provider切换 | models/providers/ | ✓ |
| 对话摘要/上下文压缩 | middlewares/summary | ✓ |
| MCP工具扩展 | agents/mcp/ | ✓ |
| 文件沙盒 | agents/backends/sandbox/ | ✓ |
