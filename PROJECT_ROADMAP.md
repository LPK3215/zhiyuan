# 智愿系统 - 项目路线图与待办事项

## 一、当前架构说明

### 现有架构
```
┌─────────────────────────────────────────────────────┐
│  前端 Vue 3 + Ant Design Vue + Vite                │
│  所有角色（superadmin / admin / user）共用一套前端   │
│  通过侧边栏菜单区分功能入口                          │
├─────────────────────────────────────────────────────┤
│  后端 FastAPI + SQLAlchemy + PostgreSQL             │
│  RBAC 权限控制：角色决定 API 访问权限                │
│  管理 API 路径：/api/zhiyuan/admin/*                │
├─────────────────────────────────────────────────────┤
│  数据层 PostgreSQL + Redis + MinIO                   │
│  数据表：universities, majors, admission_scores 等   │
└─────────────────────────────────────────────────────┘
```

### 角色权限现状
- **superadmin**：拥有所有权限，可访问所有管理功能
- **admin**：拥有管理功能的读写权限
- **user**：只能使用志愿填报、咨询等普通功能

## 二、当前问题记录

### P0 — 阻塞性问题
1. ~~首页统计数据显示为零~~ ✅ 已修复
   - 原因：前端 `zhiyuan_api.js` 中 `getPublicStats` 硬编码了很多字段为 0
   - 修复：正确映射后端 `major_count`, `admission_score_count` 等字段

### P1 — 重要问题
2. **管理端 CUD 界面缺失**
   - 现状：后端已有完整的管理 CRUD API（院校、专业、分数、计划、规则），但前端没有对应的管理界面
   - 影响：管理员无法通过 UI 管理数据，只能通过 API 或直接操作数据库
   - 建议：后续开发独立的管理后台界面（可作为独立前端应用或集成到现有系统中）

3. **数据量不足**
   - 现状：仅覆盖 5 个省份、50+ 所院校、3 年数据
   - 目标：覆盖全国 30+ 省份、200+ 所院校、5+ 年真实数据
   - 数据来源：各省教育考试院公开数据、阳光高考网等

4. **部分 API 返回 404**
   - 已知缺失的路由：`/api/auth/users/me`、`/api/zhiyuan/graph/init` 等
   - 建议：补齐缺失的 API 路由

### P2 — 体验优化
5. **同分人数未展示**
   - 后端已返回 `same_score_count`，前端 UI 未展示
   - 建议：在位次查询结果中增加同分人数展示

6. **控制台 Error 日志**
   - 多条 Error 级错误信息
   - 建议：逐步清理，提升代码质量

## 三、管理端与用户端割裂问题分析

### 问题描述
当前系统中，管理员（superadmin/admin）和普通用户登录后看到的是**同一套界面**，通过侧边栏菜单的可见性来区分权限。这种设计存在以下问题：

1. **用户体验割裂**：管理员期望看到的是数据管理界面（增删改查、统计报表、数据审核），而不是志愿填报界面
2. **功能混淆**：管理功能和用户功能混合在同一导航中，增加了认知负担
3. **扩展性差**：当管理功能增多时，前端会变得臃肿

### 建议的架构调整（后续）

**方案 A：同应用不同路由（推荐）**
```
/                          -> 用户首页（当前）
/zhiyuan/plan              -> 用户志愿填报
/zhiyuan/universities      -> 用户院校浏览
/zhiyuan/policy            -> 用户政策问答
/admin                     -> 管理后台首页
/admin/universities        -> 院校管理 CRUD
/admin/majors              -> 专业管理 CRUD
/admin/scores              -> 分数管理
/admin/plans               -> 计划管理
/admin/rules               -> 规则管理
/admin/stats               -> 数据统计
```

**方案 B：独立前端应用**
- 管理后台使用独立的前端应用（如 React + Ant Design Pro）
- 通过 API Gateway 统一认证和路由

### 建议的实施优先级
1. 先在现有应用中增加管理路由（方案 A），快速满足管理需求
2. 后续根据管理功能的复杂度，考虑拆分为独立应用

## 四、数据建设规划

### 第一阶段：扩充种子数据（当前）
- ✅ 扩充至 50+ 所院校
- ✅ 覆盖 5 个省份（河南、山东、湖北、河北、安徽）
- ✅ 3 年录取数据（2023-2025）
- ✅ 完整一分一段表

### 第二阶段：数据质量提升
- [ ] 收集真实录取分数线数据（从公开渠道）
- [ ] 补充更多省份数据
- [ ] 丰富专业介绍、就业前景信息
- [ ] 添加选科要求、体检要求等详细信息

### 第三阶段：智能化增强
- [ ] 录取概率模型优化
- [ ] 智能推荐算法迭代
- [ ] 政策语义检索增强
- [ ] 数据可视化增强

## 五、环境切换与部署说明

### 数据持久化机制
数据通过以下方式持久化在项目中：

1. **种子数据脚本**：`backend/scripts/import_zhiyuan_data.py`（基础版）和 `backend/scripts/import_enhanced_data.py`（增强版）
2. **初始化命令**：`make seed` 或 `cd backend && python scripts/import_enhanced_data.py`
3. **数据目录**：`backend/data/seed_data/` 存放数据说明和相关文件

### 环境切换步骤
```bash
# 1. 停止现有容器
make down

# 2. 清理数据库（保留数据库实例）
docker compose exec postgres psql -U postgres -c "DROP DATABASE zhiyuan;"  # 谨慎使用

# 3. 重新启动
make up

# 4. 初始化数据
make seed  # 使用基础版
# 或
cd backend && python scripts/import_enhanced_data.py  # 使用增强版

# 5. 验证
make test
```

## 六、技术债务清单

### 需要补充的测试
- [ ] 志愿方案生成接口的单元测试
- [ ] 分数转位次接口的边界测试
- [ ] 管理 CRUD 接口的集成测试
- [ ] 前端组件的 E2E 测试

### 代码质量改进
- [ ] 前端 TypeScript 类型定义完善
- [ ] 后端 API 响应格式统一化
- [ ] 错误处理和日志规范化
- [ ] 配置文件管理优化

### 安全相关
- [ ] API 限流和防刷
- [ ] 输入验证加强
- [ ] SQL 注入防护
- [ ] 敏感数据加密存储

---

*文档创建时间：2026-07-31*
*最后更新：2026-07-31*
