智愿系统是一个高考志愿填报辅助平台。

核心功能包括：
1. 院校查询：支持按省份、层次、类型搜索全国院校。
2. 专业对比：提供不同院校相同专业的录取分数对比。
3. 分数位次：根据分数查询一分一段表，获取省排名。
4. 冲稳保推荐：基于用户位次，智能推荐冲刺、稳定、保底三档院校。
5. 志愿方案：生成完整的志愿填报方案，包含每所院校的推荐专业。

系统采用 FastAPI 后端 + React 前端，使用 PostgreSQL 存储业务数据，Milvus 作为向量数据库，MinIO 作为对象存储，Redis 用于缓存和限流。
